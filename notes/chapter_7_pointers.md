# Глава 7: Указатели (Pointers)

## Что такое указатели

**Указатель** — это переменная, которая хранит адрес другой переменной в памяти.

### Сравнение с Java

В Java **все объекты передаются по ссылке** (reference), примитивы — по значению. Но сама ссылка скрыта от разработчика:

```java
// Java
Person p = new Person("Alice");  // p — это ссылка, но мы это не видим явно
changeName(p);                    // передаём ссылку неявно
```

В Go **по умолчанию всё передаётся по значению** (копируется), но у нас есть **явные указатели**, которые позволяют работать с адресом переменной:

```go
// Go
x := 10
p := &x     // p — указатель на x, хранит адрес x
*p = 20     // изменяем значение по адресу
fmt.Println(x)  // 20 — x изменился!
```

**Ключевое отличие от C/C++**: в Go нет арифметики указателей (нельзя делать `ptr++`, `ptr + 5`). Это безопаснее.

---

## Операторы & и *

### & — взятие адреса (address-of)

Оператор `&` возвращает **адрес** переменной:

```go
x := 42
p := &x  // p теперь указывает на x (хранит адрес x)

fmt.Println(x)   // 42
fmt.Println(&x)  // адрес в памяти, например: 0xc00001a0a0
fmt.Println(p)   // тот же адрес: 0xc00001a0a0
```

### * — разыменование (dereference)

Оператор `*` используется в двух контекстах:

1. **В объявлении типа** — указывает что это указатель:
```go
var p *int  // p — указатель на int
```

2. **В выражении** — получает значение по адресу (разыменование):
```go
x := 42
p := &x      // p указывает на x
fmt.Println(*p)  // 42 — значение по адресу p
*p = 100     // изменяем значение по адресу
fmt.Println(x)   // 100 — x изменился!
```

### Пример с объяснением

```go
package main

import "fmt"

func main() {
    // Обычная переменная
    x := 10
    fmt.Println("x =", x)           // x = 10
    fmt.Println("адрес x:", &x)     // адрес x: 0xc00001a0a0

    // Создаём указатель на x
    p := &x
    fmt.Println("p =", p)           // p = 0xc00001a0a0 (адрес)
    fmt.Println("*p =", *p)         // *p = 10 (значение по адресу)

    // Изменяем значение через указатель
    *p = 20
    fmt.Println("x после *p = 20:", x)  // x = 20 (изменился!)
}
```

---

## Передача по значению vs передача по ссылке

### Передача по значению (по умолчанию)

Когда передаёшь переменную в функцию, Go **копирует** её значение:

```go
func increment(n int) {
    n++  // изменяем копию
    fmt.Println("внутри функции:", n)
}

func main() {
    x := 10
    increment(x)
    fmt.Println("после функции:", x)  // 10 — не изменился!
}
```

**Вывод:**
```
внутри функции: 11
после функции: 10
```

**Аналогия с Java**: как если передавать примитив (`int`, `boolean`) в метод.

### Передача по ссылке (через указатель)

Передаём **указатель**, тогда функция может изменить оригинальное значение:

```go
func increment(n *int) {  // принимаем указатель
    *n++  // изменяем значение по адресу
    fmt.Println("внутри функции:", *n)
}

func main() {
    x := 10
    increment(&x)  // передаём адрес x
    fmt.Println("после функции:", x)  // 11 — изменился!
}
```

**Вывод:**
```
внутри функции: 11
после функции: 11
```

**Аналогия с Java**: как если передавать объект в метод — изменения внутри метода влияют на оригинал.

### Зачем это нужно?

1. **Изменение значения**: когда функция должна изменить переменную
2. **Производительность**: избегаем копирования больших структур (об этом в главе 8)
3. **Nil значения**: указатели могут быть `nil` (отсутствие значения)

---

## Zero value для указателей

Zero value (значение по умолчанию) для указателей — **nil**:

```go
var p *int
fmt.Println(p)        // <nil>
fmt.Println(p == nil) // true
```

⚠️ **Важно**: разыменование `nil` указателя вызывает **panic** (runtime ошибка):

```go
var p *int
fmt.Println(*p)  // panic: runtime error: invalid memory address
```

Всегда проверяй на `nil` перед использованием:

```go
func printValue(p *int) {
    if p == nil {
        fmt.Println("указатель nil")
        return
    }
    fmt.Println("значение:", *p)
}
```

---

## Создание указателей с new()

Встроенная функция `new(T)` выделяет память для типа `T`, инициализирует её zero value и **возвращает указатель**:

```go
p := new(int)
fmt.Println(p)   // адрес, например: 0xc00001a0a0
fmt.Println(*p)  // 0 (zero value для int)

*p = 42
fmt.Println(*p)  // 42
```

**Эквивалентный код без new()**:

```go
var x int   // x = 0 (zero value)
p := &x     // p указывает на x
```

`new()` полезен когда нужен указатель сразу, без создания промежуточной переменной.

---

## Указатели и слайсы/мапы

### Слайсы и мапы — уже ссылочные типы

В Go слайсы и мапы **внутри содержат ссылки** на данные. Поэтому передача их в функцию уже позволяет изменять содержимое:

```go
func addElement(s []int) {
    s[0] = 100  // изменяем элемент — работает!
}

func main() {
    nums := []int{1, 2, 3}
    addElement(nums)
    fmt.Println(nums)  // [100 2 3] — изменился!
}
```

**НО!** Если хочешь изменить **сам слайс** (например, через `append`), нужен указатель:

```go
func appendElement(s *[]int, val int) {
    *s = append(*s, val)  // изменяем сам слайс
}

func main() {
    nums := []int{1, 2, 3}
    appendElement(&nums, 4)
    fmt.Println(nums)  // [1 2 3 4] — работает!
}
```

Или проще — возвращать новый слайс:

```go
func appendElement(s []int, val int) []int {
    return append(s, val)
}

func main() {
    nums := []int{1, 2, 3}
    nums = appendElement(nums, 4)
    fmt.Println(nums)  // [1 2 3 4]
}
```

**Вывод**: слайсы/мапы передаются "по ссылке" для чтения/изменения элементов, но для изменения структуры слайса нужен указатель или возврат нового значения.

---

## Когда использовать указатели?

✅ **Используй указатели:**
- Когда функция должна изменить переменную
- Для больших структур (экономия памяти, об этом в главе 8)
- Когда нужно представить "отсутствие значения" (`nil`)

❌ **Не используй указатели:**
- Для примитивов (`int`, `bool`, `string`) если не нужно изменение
- Для слайсов/мап если не меняешь саму структуру
- Когда код становится менее читаемым без необходимости

**Идиома Go**: используй указатели только когда это действительно нужно. В Go принято передавать значения, если это не вредит производительности.

---

## Пример: swap функция

Классическая задача — обмен значениями двух переменных.

### Неправильно (передача по значению):

```go
func swap(a, b int) {
    a, b = b, a  // меняем копии, оригиналы не изменятся
}

func main() {
    x, y := 10, 20
    swap(x, y)
    fmt.Println(x, y)  // 10 20 — не изменились!
}
```

### Правильно (через указатели):

```go
func swap(a, b *int) {
    *a, *b = *b, *a  // меняем значения по адресам
}

func main() {
    x, y := 10, 20
    swap(&x, &y)
    fmt.Println(x, y)  // 20 10 — изменились!
}
```

### Или идиоматичнее (множественный возврат):

```go
func swap(a, b int) (int, int) {
    return b, a
}

func main() {
    x, y := 10, 20
    x, y = swap(x, y)
    fmt.Println(x, y)  // 20 10
}
```

---

## Сравнение с Java

| Java | Go |
|------|-----|
| Примитивы передаются по значению | **Всё** по умолчанию передаётся по значению |
| Объекты передаются по ссылке (неявно) | Нужен **явный указатель** для передачи по ссылке |
| `null` для объектов | `nil` для указателей |
| Нет явного доступа к адресам | `&x` — получить адрес, `*p` — получить значение |
| `Integer`, `AtomicInteger` для mutable int | Используем `*int` |

**В Java:**
```java
void increment(int n) {
    n++;  // не изменит оригинал
}

void increment(AtomicInteger n) {
    n.incrementAndGet();  // изменит
}
```

**В Go:**
```go
func increment(n int) {
    n++  // не изменит оригинал
}

func increment(n *int) {
    (*n)++  // изменит
}
```

---

## Основные правила работы с указателями

1. **`&x`** — получить адрес переменной `x`
2. **`*p`** — получить значение по адресу `p`
3. **`var p *int`** — объявить указатель на `int`
4. **`new(T)`** — выделить память и получить указатель
5. **Всегда проверяй на `nil`** перед разыменованием
6. **Нет арифметики указателей** в Go (безопасно!)
7. Используй указатели **осознанно**, не злоупотребляй

---

## Резюме

- **Указатель** хранит адрес переменной
- **`&`** — взять адрес, **`*`** — разыменовать (получить значение)
- По умолчанию в Go всё передаётся **по значению** (копируется)
- Указатели позволяют функциям **изменять оригинальные значения**
- Zero value для указателей — **`nil`**
- `new(T)` выделяет память и возвращает указатель
- Слайсы/мапы уже ссылочные для элементов, но не для структуры
- Используй указатели когда нужно изменение или экономия памяти

Готов к практическому заданию? Напиши "прочитал" или задай вопросы! 🚀
