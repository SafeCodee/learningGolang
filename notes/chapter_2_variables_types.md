# Глава 2: Переменные и базовые типы данных

## Объявление переменных

### 1. С помощью var

```go
var name string = "John"
var age int = 30
var price float64 = 99.99
var isActive bool = true
```

### 2. С type inference (вывод типа)

```go
var name = "John"      // Go сам определит тип string
var age = 30           // int
var price = 99.99      // float64
```

### 3. Короткое объявление := (внутри функций)

```go
name := "John"         // Самый частый способ
age := 30
price := 99.99
```

**Важно:** `:=` работает только внутри функций!

### 4. Множественное объявление

```go
var x, y int = 1, 2
a, b := 10, 20
name, age := "Alice", 25
```

## Базовые типы данных

### Целые числа

```go
var a int = 42           // Платформозависимый (32 или 64 бита)
var b int8 = 127         // -128 до 127
var c int16 = 32767      // -32768 до 32767
var d int32 = 2147483647
var e int64 = 9223372036854775807

var f uint = 42          // Беззнаковый
var g uint8 = 255        // 0 до 255 (byte — алиас для uint8)
```

### Числа с плавающей точкой

```go
var pi float32 = 3.14
var e float64 = 2.71828  // Более точный, используется по умолчанию
```

### Строки

```go
var greeting string = "Hello"
var multiline = `Многострочная
строка
в обратных кавычках`
```

**Важные операции со строками:**

```go
// Конкатенация
fullName := "John" + " " + "Doe"

// Длина строки (в байтах!)
length := len("Hello")  // 5

// Доступ к байтам (не символам!)
firstByte := greeting[0]  // Возвращает byte (uint8), не char!

// Строки неизменяемы (immutable) — как в Java
// greeting[0] = 'h'  // ❌ Ошибка компиляции!
```

**Важно:** `len()` возвращает количество **байтов**, не символов! Для Unicode строк это важно:
```go
s := "Привет"
len(s)  // 12 (не 6!), потому что кириллица занимает 2 байта на символ в UTF-8
```

### Символы: rune и byte

В Go два типа для символов:

```go
var b byte = 'A'      // byte = uint8, для ASCII (0-255)
var r rune = 'Ы'      // rune = int32, для Unicode code points
```

**Сравнение с Java:**
- **Java:** `char` (16 бит, UTF-16)
- **Go:** `rune` (32 бита, UTF-8), `byte` (8 бит, ASCII)

```go
// Одинарные кавычки ' ' — для символов
ch := 'A'        // rune (int32)
emoji := '😀'    // rune поддерживает любой Unicode

// Двойные кавычки " " — для строк
str := "Hello"
```

### Булевы значения

```go
var isValid bool = true
var isComplete bool = false
```

## Нулевые значения (Zero Values)

В Go **все переменные имеют значение по умолчанию** (в отличие от Java, где примитивы имеют дефолты, а объекты — null).

```go
var i int        // 0
var f float64    // 0.0
var s string     // "" (пустая строка)
var b bool       // false
```

**В Java:** неинициализированные локальные переменные — ошибка компиляции.
**В Go:** всегда есть безопасное значение по умолчанию.

## Константы

```go
const Pi = 3.14159
const MaxUsers = 100

// Множественные константы
const (
    StatusOK = 200
    StatusNotFound = 404
    StatusError = 500
)
```

**Важно:** константы вычисляются на этапе компиляции.

## Type Conversion (преобразование типов)

В Go **нет автоматического приведения типов** (в отличие от Java).

### Преобразование между числовыми типами

```go
var i int = 42
var f float64 = float64(i)   // Явное преобразование
var u uint = uint(f)

// В Java было бы: double f = i; (автоматически)
```

### Преобразование string ↔ int

Для конвертации строк в числа и обратно используется пакет `strconv`:

```go
import "strconv"

// int → string
age := 25
ageStr := strconv.Itoa(age)  // "25"

// string → int
s := "42"
num, err := strconv.Atoi(s)  // num = 42, err = nil (если успешно)
```

**Важно:** `strconv.Atoi()` возвращает **два значения**: число и ошибку. Если конвертация не удалась (например, `"abc"`), `err` будет содержать описание ошибки. Подробнее про обработку ошибок — в главе 11.

**Упрощённый вариант для начала** (если уверен, что строка валидна):
```go
num, _ := strconv.Atoi("42")  // _ игнорирует ошибку
```

## Отличия от Java

| Аспект | Java | Go |
|--------|------|-----|
| **Объявление** | `int x = 10;` | `x := 10` или `var x int = 10` |
| **Type inference** | С Java 10 (`var`) | Всегда был (`:=`) |
| **Нулевые значения** | Примитивы: дефолты, объекты: null | Всегда безопасное значение |
| **Автоприведение** | int → long → float автоматически | Только явное преобразование |
| **Константы** | `final` или `static final` | `const` |
| **Строки** | `String` (объект) | `string` (встроенный тип) |

## Именование переменных (Go Style)

В Go есть устоявшиеся правила именования:

### Регистр определяет видимость

```go
// В пакете
var PublicVar int     // Экспортируется (доступна в других пакетах)
var privateVar int    // Не экспортируется (только внутри пакета)

func PublicFunc() {}  // Экспортируется
func privateFunc() {} // Не экспортируется
```

**В Java:** `public/private` ключевые слова
**В Go:** **заглавная буква** = public, **строчная** = private

### Стиль именования

```go
// camelCase для локальных переменных и приватных полей
userName := "John"
maxRetries := 3

// PascalCase для экспортируемых (public)
type User struct {
    FirstName string  // Экспортируется
    lastName  string  // Не экспортируется
}

// Короткие имена в коротких функциях — идиоматично
for i := 0; i < 10; i++ {
    // i вместо index
}

// Общепринятые сокращения
err   // error
ok    // boolean флаг
i, j  // индексы
s     // string
```

**Не используй:**
- `snake_case` (не принято в Go)
- Венгерскую нотацию (`strName`, `intAge`)
- Избыточные имена (`userNameString`)

## Важные моменты

✅ **`:=` — самый частый способ** объявления переменных
✅ **Нет null для примитивов** — всегда есть zero value
✅ **Явное преобразование типов** — безопаснее, но многословнее
✅ **string — встроенный тип**, а не класс как в Java
✅ **Регистр первой буквы** определяет видимость (public/private)
✅ **Короткие имена** в коротких функциях — нормально и идиоматично
