# Глава 12: Пакеты и модули

## Что такое пакеты в Go?

**Пакет (package)** — это способ организации и переиспользования кода в Go. Все `.go` файлы в одной директории должны принадлежать одному пакету.

**Сравнение с Java:**
- В Java: пакет = namespace (например, `com.example.myapp`)
- В Go: пакет = директория с `.go` файлами
- В Java пакеты многоуровневые (`a.b.c`), в Go — плоские (каждая директория = отдельный пакет)

## Зачем нужны пакеты?

1. **Организация кода** — разделение логики по папкам
2. **Переиспользование** — один пакет можно импортировать в разных проектах
3. **Инкапсуляция** — контроль видимости (экспортируемые vs приватные идентификаторы)

---

## Создание собственного пакета

### Структура проекта

```
myproject/
├── go.mod          # Определяет модуль (как pom.xml в Maven)
├── main.go         # Точка входа (package main)
└── mathutil/       # Наш пакет
    └── mathutil.go # Код пакета
```

### Пример пакета: `mathutil/mathutil.go`

```go
package mathutil

// Add складывает два числа
// Начинается с заглавной буквы → экспортируется (public)
func Add(a, b int) int {
    return a + b
}

// multiply умножает два числа
// Начинается со строчной буквы → не экспортируется (private)
func multiply(a, b int) int {
    return a * b
}
```

**Важно:**
- **Заглавная буква** → функция/переменная/тип **экспортируется** (доступна извне пакета)
- **Строчная буква** → **не экспортируется** (приватная, только внутри пакета)

**Сравнение с Java:**
```java
// Java
public class MathUtil {
    public int add(int a, int b) { ... }      // public
    private int multiply(int a, int b) { ... } // private
}
```

В Go нет ключевых слов `public`/`private` — видимость определяется **регистром первой буквы**.

---

## Импорт пакетов

### main.go

```go
package main

import (
    "fmt"
    "myproject/mathutil" // Импортируем наш пакет
)

func main() {
    result := mathutil.Add(5, 3) // Вызываем экспортированную функцию
    fmt.Println(result)          // 8

    // mathutil.multiply(2, 3) // ❌ Ошибка компиляции! multiply не экспортируется
}
```

**Путь импорта:**
- Формат: `module_name/path/to/package`
- Начинается с имени модуля из `go.mod`

---

## Go модули (Go Modules)

**Модуль** — это коллекция пакетов с версионированием. Появился в Go 1.11 (заменил старый GOPATH).

**Сравнение с Java:**
- Go модуль ≈ Maven/Gradle проект
- `go.mod` ≈ `pom.xml` / `build.gradle`
- `go.sum` ≈ `pom.xml.lock` / `gradle.lock` (контрольные суммы зависимостей)

### Создание модуля

```bash
# Инициализация модуля
go mod init myproject

# Для публичного проекта на GitHub используй полный путь:
go mod init github.com/username/myproject
```

После этого создаётся файл `go.mod`:

```
module myproject

go 1.21
```

### Файл go.mod

Пример с зависимостями:

```
module github.com/username/myproject

go 1.21

require (
    github.com/gorilla/mux v1.8.0
    github.com/lib/pq v1.10.9
)
```

**Структура:**
- `module` — имя модуля
- `go` — минимальная версия Go
- `require` — список зависимостей (библиотеки + версии)

### Файл go.sum

Содержит контрольные суммы (хеши) зависимостей для безопасности:

```
github.com/gorilla/mux v1.8.0 h1:i40aqfkR1h2SlN9hojwV5ZA91wcXFOvkdNIeFDP5koI=
github.com/gorilla/mux v1.8.0/go.mod h1:DVbg23sWSpFRCP0SfiEN6jmj59UnW/n46BH5rLB71So=
```

**Не редактируй вручную!** Управляется автоматически командой `go mod`.

---

## Работа с зависимостями

### Добавление зависимости

**Способ 1:** Импортировать в коде и запустить `go mod tidy`

```go
package main

import (
    "fmt"
    "github.com/gorilla/mux" // Новая зависимость
)

func main() {
    r := mux.NewRouter()
    fmt.Println(r)
}
```

```bash
go mod tidy  # Скачает зависимость и обновит go.mod
```

**Способ 2:** Добавить явно

```bash
go get github.com/gorilla/mux@v1.8.0  # Указать версию
go get github.com/gorilla/mux@latest  # Последняя версия
```

### Удаление неиспользуемых зависимостей

```bash
go mod tidy  # Удалит зависимости, которые не импортируются в коде
```

### Обновление зависимостей

```bash
go get -u ./...          # Обновить все зависимости
go get -u github.com/gorilla/mux  # Обновить конкретную
```

### Просмотр зависимостей

```bash
go list -m all          # Список всех зависимостей
go mod graph            # Граф зависимостей
go mod why github.com/lib/pq  # Почему нужна эта зависимость?
```

---

## Семантическое версионирование (SemVer)

Go использует **SemVer**: `MAJOR.MINOR.PATCH`

Пример: `v1.8.0`
- `v1` — мажорная версия (breaking changes)
- `8` — минорная версия (новые функции, обратная совместимость)
- `0` — патч (исправления багов)

**Правила:**
- Версия `v0.x.x` — нестабильная (могут быть breaking changes)
- Версия `v1.x.x` — стабильная API
- `v2`, `v3` и т.д. — новые мажорные версии (breaking changes)

**Импорт мажорных версий > v1:**

```go
import "github.com/some/package/v2"  // Для v2.x.x
import "github.com/some/package/v3"  // Для v3.x.x
```

---

## Структура пакетов: best practices

### Пример реального проекта

```
mywebapp/
├── go.mod
├── go.sum
├── main.go                 # Точка входа
├── handlers/               # HTTP обработчики
│   └── user_handler.go
├── models/                 # Структуры данных
│   └── user.go
├── database/               # Работа с БД
│   └── db.go
└── utils/                  # Утилиты
    └── logger.go
```

### Правило: один пакет = одна директория

**❌ Неправильно:**
```
models/
├── user.go       # package models
└── product.go    # package products  ← Ошибка!
```

**✅ Правильно:**
```
models/
├── user.go       # package models
└── product.go    # package models
```

Все `.go` файлы в директории `models/` должны иметь `package models`.

---

## Импорт: типичные сценарии

### Стандартная библиотека

```go
import "fmt"         // Пакет из стандартной библиотеки
import "net/http"    // Вложенный пакет
```

### Внешние зависимости

```go
import "github.com/gorilla/mux"
```

### Локальные пакеты

```go
import "myproject/handlers"
import "myproject/models"
```

### Алиасы (переименование)

```go
import (
    "math/rand"
    cryptorand "crypto/rand"  // Алиас, чтобы избежать конфликта имён
)

func main() {
    n := rand.Intn(10)           // math/rand
    cryptorand.Read([]byte{})    // crypto/rand
}
```

### Импорт только для side effects

```go
import _ "github.com/lib/pq"  // Только для вызова init()
```

Используется когда нужно выполнить функцию `init()` пакета, но не использовать его функции напрямую (например, регистрация драйвера БД).

---

## Функция init()

Каждый пакет может иметь функцию `init()`, которая выполняется автоматически при импорте:

```go
package database

import "fmt"

func init() {
    fmt.Println("Инициализация пакета database")
}
```

**Порядок выполнения:**
1. Инициализация переменных пакета
2. Вызов `init()` (если есть)
3. Вызов `main()` (только в `package main`)

**Важно:** `init()` вызывается только один раз, даже если пакет импортируется в нескольких местах.

---

## Внутренние пакеты (internal/)

Специальная директория `internal/` — её содержимое **не экспортируется** за пределы родительского модуля.

```
myproject/
├── go.mod
├── main.go
└── internal/
    └── secret/
        └── secret.go  # Доступно только внутри myproject
```

**Другие проекты не смогут импортировать `myproject/internal/secret`.**

**Сравнение с Java:**
- В Java: package-private классы
- В Go: `internal/` — пакеты доступны только внутри модуля

---

## Vendor (опционально)

`vendor/` — локальная копия всех зависимостей в проекте.

```bash
go mod vendor  # Скопировать зависимости в vendor/
```

**Зачем?**
- Гарантировать одинаковые зависимости на всех машинах
- Работа без интернета

**Сейчас редко используется** — `go.sum` обеспечивает воспроизводимость сборки.

---

## Команды go mod: шпаргалка

```bash
go mod init <module>      # Создать новый модуль
go mod tidy               # Добавить недостающие / удалить неиспользуемые зависимости
go get <package>@<version> # Добавить/обновить зависимость
go get -u ./...           # Обновить все зависимости
go list -m all            # Список зависимостей
go mod download           # Скачать зависимости без установки
go mod vendor             # Создать vendor/ с копией зависимостей
go mod verify             # Проверить контрольные суммы зависимостей
```

---

## Ключевые отличия от Java

| Аспект                  | Go                                  | Java                                    |
|-------------------------|-------------------------------------|-----------------------------------------|
| **Файл конфигурации**   | `go.mod`                            | `pom.xml` / `build.gradle`              |
| **Менеджер пакетов**    | Встроенный (`go mod`)               | Maven / Gradle                          |
| **Видимость**           | Регистр первой буквы (A/a)          | `public` / `private` / `protected`      |
| **Структура пакетов**   | Пакет = директория                  | Пакет = namespace (многоуровневый)      |
| **Импорт**              | Путь от корня модуля                | Fully qualified name                    |
| **Зависимости**         | Автоматически в `go.mod`            | Явно в `pom.xml`                        |
| **Версионирование**     | Семантическое (SemVer)              | Любое (обычно SemVer)                   |

---

## Пример: структура реального проекта

```
github.com/username/myapp/
├── go.mod
├── go.sum
├── main.go                   # package main
├── handlers/
│   ├── user.go               # package handlers
│   └── product.go            # package handlers
├── models/
│   ├── user.go               # package models
│   └── product.go            # package models
├── database/
│   └── postgres.go           # package database
├── internal/
│   └── auth/
│       └── token.go          # package auth (только для myapp)
└── utils/
    └── logger.go             # package utils
```

**main.go:**

```go
package main

import (
    "fmt"
    "github.com/username/myapp/handlers"
    "github.com/username/myapp/models"
)

func main() {
    user := models.User{Name: "Alice"}
    handlers.CreateUser(user)
    fmt.Println("App started")
}
```

---

## Резюме

✅ **Пакет** — это директория с `.go` файлами одного пространства имён
✅ **Модуль** — коллекция пакетов с версионированием (`go.mod`)
✅ **Экспорт** — заглавная буква в имени (публичный), строчная (приватный)
✅ **Зависимости** — управляются через `go get` и `go mod tidy`
✅ **SemVer** — стандарт версионирования (`v1.2.3`)
✅ **internal/** — пакеты доступны только внутри модуля
✅ **Без Maven/Gradle** — всё встроено в `go mod`

**Философия Go:**
- Простота (нет сложных конфигураций как в Maven)
- Явность (все зависимости в `go.mod`)
- Минимализм (один способ делать вещи)

---

Готов переходить к практике! 🚀
